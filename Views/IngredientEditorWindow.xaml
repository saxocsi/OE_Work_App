<Window x:Class="OE_Work_App.Views.IngredientEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingredient Editor"
        Height="260"
        Width="400"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Name"/>

        <TextBox Grid.Row="1"
                 Margin="0 5 0 10"
                 Text="{Binding Name}"/>

        <TextBlock Grid.Row="2"
                   Text="CL / adag"/>

        <StackPanel Grid.Row="3">
            <TextBox Margin="0 5 0 10"
                     Text="{Binding Cl}"/>

            <TextBlock Text="Price / adag"/>

            <TextBox Margin="0 5 0 10"
                     Text="{Binding Price}"/>
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Save"
                    Width="90"
                    Margin="0 0 10 0"
                    Click="SaveBtn_Click"/>

            <Button Content="Cancel"
                    Width="90"
                    Click="CancelBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>