<Window x:Class="OE_Work_App.Views.IngredientEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ingredient Editor"
        Height="280"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Ingredient name"/>

        <TextBox Grid.Row="1"
                 Margin="0 5 0 10"
                 Text="{Binding IngredientName, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="2">

            <TextBlock Text="CL / serving"/>

            <TextBox Margin="0 5 0 10"
                     Text="{Binding Cl, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Price / serving"/>

            <TextBox Margin="0 5 0 10"
                     Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Save"
                    Width="100"
                    Margin="0 0 10 0"
                    Click="SaveBtn_Click"/>

            <Button Content="Cancel"
                    Width="100"
                    Click="CancelBtn_Click"/>

        </StackPanel>

    </Grid>

</Window>